import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  AndroidSafeArea: {
    flex: 1,
    backgroundColor: '#f9f3f1',
    paddingTop: 0,
    paddingStart: 20,
    paddingEnd: 20,
  },
});
